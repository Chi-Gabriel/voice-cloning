# Qwen-TTS API Configuration

# Security
API_KEY=your_secret_api_key_here

# Model Loading (set to False to save VRAM)
ENABLE_VOICE_DESIGN=True
ENABLE_VOICE_CLONE=True
ENABLE_CUSTOM_VOICE=True

# Device
DEVICE=cuda:0

# Model Path (if different from default shared path)
# MODEL_ROOT=/app/models/Qwen3-TTS
